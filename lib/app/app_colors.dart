import 'package:flutter/material.dart';

class AppColors{
  static final ColorScheme colorScheme = ColorScheme.fromSeed(seedColor: AppColors.blue);

  static const Color blue = Colors.blueAccent;  
  static const Color white = Colors.white; 
  static const Color red = Color.fromARGB(255, 173, 20, 20); 
  
  
}